/**
 * @venus-library mocha
 */

describe('Array', function() {
  describe('join', function() {
    it('should join values in array with commas', function() {
      expect('a').to.eql('a');
    });
  });
});
